======
Task 1
======
The first error was because the multiply function didn't exist due to it being named multiple.
The second error was because prime was returning false if n > 2 instead of if n < 2.
The third error was because is even was returning true if the n%2 is 1.
The fourth error was because subtraction was performing b - a instead of a - b.

Most of these errors were quite easy to detect, the most difficult was probably the error in is_prime.
The only improvement I can think of is to use an abs() in is_even to make the comparison more consistent.
Most of my experience with testing is from java and provided tests in systems, but I do typically perform black box testing. Simply checking functionality and adding prints for debugging.

======
Task 2
======
I would say that knowing whats covered in the code, the most likely places to hide bugs are the return values that perform some calculations. 
For example, the return of get_win_rate or apply_bonus could hide incorrect calculations. Yet, without being able to view what is covered with 
error raises I would say recording a match between a non-existant player or adding a negative bonus.

I covered all branched, so no modifications need to be made.

The experience was quite similar except for this task, I wasn't looking at failing tests but missed coverage.
Having coverage in task 1 wouldn't have pointed me to the buggy lines because they were covered. Test coverage 
can help ensure you aren't missing import function results, such as returns, error checks, and value assignments.

======
Task 3
======
Can you generate test cases for sample_app/quiz.py? Try to cover as many lines and branches as possible.
Ensure you cover function returns, value assignments, and error raises. Use pytest to create the tests.
I struggled in deciding what information to give the model because I wasn't sure what tools the model had
access too.

It couldn't view the actual code file, so it just created some generic tests. It did good at giving a
comprehensive output, but it wasn't really on specific to my case. I wouldn't accept these tests as is
because they aren't specific to my code.

The tests it generated weren't applicable to the supplied quiz.py since it couldn't see the file. Thus, I
was unable to run them.

I have used claude code to generate test cases for research and projects in classes.

======
Task 4
======
The agent took to long to run so I couldn't run it

The agentic approach is much easier than the approach in task 3. Since it has access to tools to view and edit
your codebase it required minimal prompt engineering to get a good output. The skills also make it perform better
giving it clear goals to aim for. Even, without those skills my experience working with coding agents is that it
is quite easy to fix issues if you supply them with a little feedback and location for more info on what needs fixing.

I told it to ensure it has 100% coverage, avoids vague assertions and names, and keeps tests fast. I find these to be
the most crucial in making tests.

I am not sure what to add since claude code is pretty capable already.

I think a lot of maintanence will be performed by AI but still require human supervision.